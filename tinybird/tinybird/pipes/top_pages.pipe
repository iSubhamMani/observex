DESCRIPTION "Top pages by views and unique visitors"

TOKEN "top_pages_read_token" READ

NODE endpoint
SQL >
    %
    SELECT
        pathname as path,
        count() as views,
        uniq(visitorId) as visitors
    FROM incoming_events
    WHERE websiteId = {{ String(websiteId, required=true) }}
        AND event_name = 'pageview'
        AND timestamp >= {{ DateTime(start, required=true) }}
        AND timestamp < {{ DateTime(end, required=true) }}
    GROUP BY pathname
    ORDER BY views DESC
    LIMIT {{ Int32(limit, 10) }}
TYPE endpoint